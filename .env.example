# Environment variables for Face ID Verification API

# API Settings
APP_NAME=Face ID Verification API
APP_VERSION=1.0.0
DEBUG=False

# Model Settings
MODEL_NAME=buffalo_l
CTX_ID=0  # -1 for CPU, 0 for GPU (if available)

# File Upload Settings
MAX_FILE_SIZE=10485760  # 10MB in bytes

# Face Detection Settings
MIN_QUALITY_THRESHOLD=25
HIGH_QUALITY_THRESHOLD=60
LOW_QUALITY_THRESHOLD=70
HIGH_SIMILARITY_THRESHOLD=0.33
DEFAULT_SIMILARITY_THRESHOLD=0.45

# Random Confidence Settings (for testing/development)
USE_RANDOM_CONFIDENCE=False  # Set to True to use random confidence generation
RANDOM_CONFIDENCE_MATCH_MIN=70.0
RANDOM_CONFIDENCE_MATCH_MAX=85.0
RANDOM_CONFIDENCE_NO_MATCH_MIN=30.0
RANDOM_CONFIDENCE_NO_MATCH_MAX=45.0
